import { Header } from "components";

const AllUsers = () => {
  return (
    <main className="dashboard wrapper">
      <Header
        title={`Trips Page`}
        description="Check out our currrent users in real time."
      />
    </main>
  );
};

export default AllUsers;
